// Optional: shared like/dislike counts. Leave both empty and votes stay in each visitor's own browser.
// Both values are safe to publish: the "anon" key only allows what supabase.sql permits.
window.HUB_CONFIG = {
  SUPABASE_URL: "",   // e.g. "https://abcdxyz.supabase.co"
  SUPABASE_KEY: ""    // the project's "anon public" key
};
